var names = document.getElementById("names");
// names.options.length = 4;
// names.options.length = 0;

//names.removeChild(names.firstElementChild);

//names.remove(0);

names.options[0] = null;